import type { Metadata, Viewport } from 'next'
import { Playfair_Display, Inter } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const playfair = Playfair_Display({ 
  subsets: ["latin"],
  variable: '--font-playfair',
  display: 'swap',
})

const inter = Inter({ 
  subsets: ["latin"],
  variable: '--font-inter',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Elegance Events | Premium Event Planning & Celebrations',
  description: 'Create unforgettable moments with our bespoke event planning services. From weddings to corporate galas, we craft extraordinary celebrations tailored to your vision.',
  keywords: ['event planning', 'wedding planner', 'luxury events', 'corporate events', 'celebrations', 'party planning'],
  openGraph: {
    title: 'Elegance Events | Premium Event Planning',
    description: 'Crafting unforgettable celebrations with elegance and sophistication',
    type: 'website',
  },
}

export const viewport: Viewport = {
  themeColor: '#1a1a1a',
  width: 'device-width',
  initialScale: 1,
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable} bg-background`}>
      <body className="font-sans antialiased">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
